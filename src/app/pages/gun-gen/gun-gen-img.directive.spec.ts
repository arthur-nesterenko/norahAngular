import { GunGenImgDirective } from './gun-gen-img.directive';

describe('GunGenImgDirective', () => {
  it('should create an instance', () => {
    const directive = new GunGenImgDirective();
    expect(directive).toBeTruthy();
  });
});
